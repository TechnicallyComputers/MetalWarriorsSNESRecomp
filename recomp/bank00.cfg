# Metal Warriors (USA) — LLE-first seed
# Progressive names: edit recomp/symbols.toml, then tools/sync_symbols.py
# (also run automatically by tools/regen.sh). See docs/SYMBOLS.md.
bank = 0
auto_vectors
tier_down_stubs

# >>> BEGIN symbols.toml (generated — do not edit)
# Auto-synced from recomp/symbols.toml (bank $00).
# Set emit=true on a [[func]] to promote it into AOT codegen.
func I_RESET 80AE  # Emulation RESET vector ($FFFC)
func I_NMI 8182  # Native NMI trampoline — PHK/PER/JML [$0000]/RTI
func I_IRQ 8224  # Native IRQ vector ($FFEE)
# <<< END symbols.toml
